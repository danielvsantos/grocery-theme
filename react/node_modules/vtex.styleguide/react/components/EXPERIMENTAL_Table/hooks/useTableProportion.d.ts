import { Column } from '../index';
export default function useTableProportion({ columns, ratio }: ProportionData): {
    sizedColumns: {
        width: string;
        id?: string;
        title?: string | Function | Element;
        cellRenderer?: (cellData: import("..").CellData) => import("react").ReactNode;
    }[];
};
declare type ProportionData = {
    columns: Array<Column>;
    ratio: Array<number>;
};
export {};
