interface Props {
    items: Item[];
    loading: boolean;
    onQuantityChange: (uniqueId: string, value: number, item?: Item) => void;
    onRemove: (uniqueId: string, item?: Item) => void;
}
declare const ProductList: StorefrontFunctionComponent<Props>;
export default ProductList;
