import { ReactNode } from 'react';
interface InsertAddressResult {
    success: boolean;
    orderForm: any;
}
interface Context {
    canEditData: boolean;
    countries: string[];
    deliveryOptions: DeliveryOption[];
    insertAddress: (address: CheckoutAddress) => Promise<InsertAddressResult>;
    loading: boolean;
    numberOfItems: number;
    numberOfUnavailableItems: number;
    selectDeliveryOption: (option: string) => void;
    selectedAddress: Address;
}
export declare const useShipping: () => Context;
declare const Shipping: StorefrontFunctionComponent<ShippingProps>;
interface ShippingProps {
    children: ReactNode;
    canEditData: boolean;
    countries: string[];
    deliveryOptions: DeliveryOption[];
    insertAddress: (address: CheckoutAddress) => Promise<InsertAddressResult>;
    loading: boolean;
    numberOfItems: number;
    numberOfUnavailableItems: number;
    selectDeliveryOption: (option: string) => void;
    selectedAddress: Address;
    title: string;
}
export default Shipping;
