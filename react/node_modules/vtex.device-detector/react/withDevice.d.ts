import React from 'react';
import { DeviceInfo } from './useDevice';
interface WithDeviceProps {
    deviceInfo: DeviceInfo;
}
declare const withDevice: <P extends object>(Component: React.ComponentType<P>) => React.FC<P & WithDeviceProps>;
export default withDevice;
