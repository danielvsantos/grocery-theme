import { Props as RowProps } from './Row';
import { BlockClass } from '@vtex/css-handles';
interface Props extends RowProps {
    fullWidth?: boolean;
}
declare const FlexLayout: StorefrontFunctionComponent<Props & BlockClass>;
export default FlexLayout;
