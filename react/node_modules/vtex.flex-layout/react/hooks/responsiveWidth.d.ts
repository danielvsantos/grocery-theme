import React from 'react';
interface DistributedWidthOptions {
    preserveLayoutOnMobile?: boolean;
}
export declare const useResponsiveWidth: (children: React.ReactNode, options: DistributedWidthOptions) => {
    cols: {
        element: React.ReactNode;
        width: string;
        hasDefinedWidth: boolean;
    }[];
    breakOnMobile: boolean;
};
export {};
