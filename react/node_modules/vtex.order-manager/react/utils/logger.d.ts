interface LogConfig {
    level: 'Critical' | 'Important' | 'Debug';
    type: 'Error' | 'Warn' | 'Info';
    workflowType: string;
    workflowInstance: string;
    event: any;
}
export declare const logSplunk: (config: LogConfig) => void;
export {};
